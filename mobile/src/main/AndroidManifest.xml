<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
        Pre-S BLE scan requires fine location at runtime; coarse alone is
        not sufficient on API <= 30 (BLUETOOTH_SCAN doesn't exist yet).
        Scoped via maxSdkVersion so API 31+ relies on BLUETOOTH_SCAN.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" android:minSdkVersion="34" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Package visibility for targeted broadcasts to xDrip+ (see
        :db's XdripBroadcastSender). Android 11+ requires the sender
        to declare a <queries> entry before `intent.package = "..."`
        will deliver; without this the broadcast is silently dropped.
    -->
    <queries>
        <package android:name="com.eveningoutpost.dexdrip" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:debuggable="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pump"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WearX2"
        tools:ignore="HardcodedDebugMode"
        android:foregroundServiceType="connectedDevice"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WearX2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:label="WearX2">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com_jwoglom_controlx2"
                    android:host="controlx2"
                    android:pathPrefix="/" />
            </intent-filter>

            <intent-filter>
                <action android:name="com.jwoglom.controlx2.action.OPEN_BOLUS" />
                <action android:name="com.jwoglom.controlx2.action.OPEN_TEMP_RATE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <service
            android:name=".CommService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"
            android:icon="@drawable/pump">
            <intent-filter>
                <!--action
                    android:name="com.google.android.gms.wearable.BIND_LISTENER"
                    tools:ignore="WearableBindListener" /-->
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/to-phone" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/from-pump" />
            </intent-filter>
        </service>

        <service
            android:name=".MobileClientService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"
            android:icon="@drawable/pump">
        </service>

        <receiver
            android:name=".BolusNotificationBroadcastReceiver"
            android:exported="false">
            <!--intent-filter>
                <action
                    android:name="com.google.android.gms.wearable.BIND_LISTENER"
                    tools:ignore="WearableBindListener" />
            </intent-filter-->
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.jwoglom.controlx2"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            android:name="android.app.shortcuts"
            android:resource="@xml/shortcuts" />
    </application>

</manifest>
